import { ListaPlatoPedidos } from './lista-plato-pedidos';

describe('ListaPlatoPedidos', () => {
  it('should create an instance', () => {
    expect(new ListaPlatoPedidos()).toBeTruthy();
  });
});
